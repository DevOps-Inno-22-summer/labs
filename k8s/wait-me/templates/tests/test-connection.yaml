apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wait-me.fullname" . }}-test-connection"
  labels:
    {{- include "wait-me.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wait-me.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
