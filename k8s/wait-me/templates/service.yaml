apiVersion: v1
kind: Service
metadata:
  name: {{ include "wait-me.fullname" . }}
  labels:
    {{- include "wait-me.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wait-me.selectorLabels" . | nindent 4 }}
