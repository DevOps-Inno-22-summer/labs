apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flutter-app.fullname" . }}-test-connection"
  labels:
    {{- include "flutter-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flutter-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
